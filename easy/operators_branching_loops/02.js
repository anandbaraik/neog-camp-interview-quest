/*
* Write a program to take a number input from user and determine whether the number is odd or
* even.
*/
let num1  = 4;

if(num1%2 == 0)
    console.log(`${num1} is even.`);
else
    console.log(`${num1} is odd.`);